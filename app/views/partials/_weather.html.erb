<div class="col s3">
  <div class="card horizontal">
    <div class="card-stacked">
      <div class="card-content">
        <p>Погода </p>
        <% @weatherArray.each do |weather| %>
          <span> <%= weather.title %>></span>
        <% end %>
      </div>
    </div>
  </div>
</div>